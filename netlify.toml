[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: alle paden naar index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
